{
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.15"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "sitemap": "tsx docs/.vitepress/sitemap.ts",
    "build:optimized": "npm run sitemap && npm run build"
  }
}